{
  "name": "Agentic Workshop",
  "image": "mcr.microsoft.com/devcontainers/universal:2",
  "customizations": {
    "vscode": {
      "extensions": [
        "GitHub.copilot",
        "GitHub.copilot-chat"
      ]
    }
  },
  "postCreateCommand": "npm install -g @fission-ai/openspec@latest && pip install pytest-playwright flask && playwright install --with-deps chromium"
}
